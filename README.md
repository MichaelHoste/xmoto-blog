XMoto.io blog
============================

`bundle install`

`jekyll build`

`bundle exec foreman start`

Use in development

`http://127.0.0.1:4000`

Deploy on server (first configure server with nginx.conf)

`./deploy.sh`
